never missed this step
1. The right location is inside ur project
 navigated to the right location on terminal
 2. Initialize a empty repo --> git init

Whenever when u work with git u work with branches
